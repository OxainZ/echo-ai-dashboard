# Copy to .streamlit/secrets.toml locally, or set in the Streamlit Cloud
# Secrets UI for the deployed app. NEVER commit the real file.
# Generate a hash:  python -c "import hashlib;print(hashlib.sha256(b'YOUR-PASSWORD').hexdigest())"
password_hash = "PASTE-SHA256-HEX-HERE"
